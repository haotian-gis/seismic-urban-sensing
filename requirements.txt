# Python >= 3.9
numpy
scipy
pandas>=1.5,<3
pyarrow          # Feather I/O
obspy
joblib
geopandas
shapely
pymongo          # SafeGraph weekly patterns stored in MongoDB
# GDAL is only needed for environment.snow_depth_mean(); install via conda:
#   conda install -c conda-forge gdal
